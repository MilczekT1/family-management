spring:
  application.name: family-management
  profiles:
    active: test
  security:
    user:
      name: "testUserName"
      password: "testUserPassword"
  cloud:
    config:
      enabled: false
  main:
    allow-bean-definition-overriding: true
budget:
  chassis:
    http-logging:
      enabled: true
