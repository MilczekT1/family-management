spring:
  application.name: family-management
  profiles:
    active: test
  security:
    user:
      name: "testUserName"
      password: "testUserPassword"
  cloud:
    config:
      enabled: false
    zipkin:
      enabled: false
budget:
  chassis:
    http-logging:
      enabled: true
